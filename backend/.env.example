# ===========================================
# Advertly Backend - Environment Configuration
# ===========================================

# Server
PORT=5000
NODE_ENV=development

# Database
MONGODB_URI=mongodb://localhost:27017/advertly

# Authentication (REQUIRED)
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your_jwt_secret_here_min_32_chars

# CORS - Allowed Origins (comma-separated for production)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001

# OpenAI API (REQUIRED for AI features)
OPENAI_API_KEY=sk-your-openai-api-key
OPENAI_MODEL=gpt-4o

# Facebook Ads Library API (Optional - for real competitor data)
# To get access token:
# 1. Create a Facebook App at developers.facebook.com
# 2. Add "Ads Management Standard Access" to your app
# 3. Get long-lived access token from Graph API Explorer
# 4. Your Business Manager must be verified
# If not configured, AI will simulate competitor analysis
FB_ACCESS_TOKEN=

# Twilio SMS (Optional - for phone verification)
# TWILIO_ACCOUNT_SID=your_twilio_account_sid
# TWILIO_AUTH_TOKEN=your_twilio_auth_token
# TWILIO_PHONE_NUMBER=+1234567890
